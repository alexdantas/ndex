#!/usr/bin/env ruby

require 'pet'
require 'pet/version'

begin
  if ARGV.empty?
    puts "pet #{PET::VERSION}"
    puts "Usage:"
    puts " $ pet pokemon_number"
    exit 1
  end

  PET::run ARGV.first.to_i
end

